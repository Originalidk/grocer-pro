/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'media.nedigital.sg',
            'img.rtacdn-os.com',
        ],
    },
};

export default nextConfig;
